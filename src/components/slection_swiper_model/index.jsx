import React,{memo} from "react";
import PropTypes from 'prop-types'

const selectionSwiperModel = memo((props)=>{
    const {  } = props
    
    return (
        <div> selectionSwiperModel</div>
    )
})

selectionSwiperModel.prototypes = {

}

export default selectionSwiperModel
