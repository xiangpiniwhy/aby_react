import React,{memo} from "react";
import PropTypes from 'prop-types'

const selectionFooterName = memo((props)=>{
    const {  } = props
    
    return (
        <div> selectionFooterName</div>
    )
})

selectionFooterName.prototypes = {

}

export default selectionFooterName
